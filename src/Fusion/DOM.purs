module Fusion.DOM (
  module Exports,
  blank
) where

import React.Spaces hiding (empty) as Exports
import React.Spaces.DOM as Exports

import React.Spaces (empty) as React.Spaces

blank = React.Spaces.empty
